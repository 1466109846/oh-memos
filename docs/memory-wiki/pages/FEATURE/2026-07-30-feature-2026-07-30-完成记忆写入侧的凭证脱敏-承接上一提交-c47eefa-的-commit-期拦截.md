---
generator: oh-memos-wiki-export
id: 07a8c3e9-117a-4606-bf5e-bbdb2f4bb45d
type: FEATURE
status: activated
tags: ["FEATURE"]
confidence: 0.99
created: 2026-07-30T16:39:00.370398000+00:00
updated: 2026-07-30T16:39:00.371398000+00:00
---

# [FEATURE] 2026-07-30 完成记忆写入侧的凭证脱敏(承接上一提交 c47eefa 的 commit 期拦截,这次是运行期拦截)。

2026-07-30 完成记忆写入侧的凭证脱敏(承接上一提交 c47eefa 的 commit 期拦截,这次是运行期拦截)。

组成:
1. src/oh_memos/security/redact.py —— 13 条模式,按结构前缀锚定(AKIA/sk-/sk-ant-/ghp_/github_pat_/eyJ../xox./AIza/BEGIN PRIVATE KEY 等)而非熵值,避免误伤普通代码与散文。顺序敏感:sk-ant- 必须在裸 sk- 之前,github_pat_ 必须在 gh[pousr]_ 之前。redact_text/redact_obj 均返回 (cleaned, labels) 对称契约。开关 MEMOS_REDACT_SECRETS=false。模块只依赖 os + re。
2. start_api.py 接入两条写路径:POST /memories(memory_content 与 messages[].content,经 _redact_text_or_log 辅助函数)与 PUT /memories/{cube}/{id}(经 redact_obj,此前完全没覆盖)。导入改为模块级。日志只记录凭证**种类**不记录值,否则告警本身就是泄漏。
3. tests/test_redact.py —— 63 项,含检测、模式顺序、误报防护(password=input()、tokenizer_path、UUID、commit SHA、路径、占位符)、幂等、开关、redact_obj 递归。
4. .githooks/pre-commit 加窄豁免 tests/test_redact.py(按精确路径,不豁免整个 tests/),否则该文件里的合成密钥会被自己的钩子拦住。

端到端验证(API 重启后,探针 cube):POST 一条含三个假密钥的记忆,库中 sources 存为 sk-pro…[REDACTED:openai_api_key]、q7Wx2Z…[REDACTED:labelled_credential]、AKIAIO…[REDACTED:aws_access_key];LLM 提炼出的正文只写「以 sk-pro 开头的密钥」,证明模型自始至终未见明文 —— 脱敏确实发生在提炼与向量化之前。探针 cube 的记忆、目录、Qdrant collection 已全部清理。

范围决定:仅覆盖写入侧。存量 98+ 条旧记忆不做读取侧脱敏、也不做扫描清洗(用户决定)。因此若旧记忆中已躺有密钥,search 仍会读回上下文。

注意:logger.warning 未出现在 .memos/logs/memos.log(该文件自重启后未增长),脱敏告警应在 API 控制台窗口。

未提交(用户未要求)。另发现既有问题:remote.origin.url 内嵌 GitHub token,明文存于 .git/config,建议轮换并改用凭证助手或 SSH。
