---
generator: oh-memos-wiki-export
id: 053c8a2a-746f-487c-bf7c-c0ad441c5bd6
type: PROGRESS
status: activated
tags: ["batch delete", "backup management", "memory-admin GUI", "memory-admin.bat", "cube deletion", "Neo4j", "Qdrant", "filesystem", "HTTP 400", "safety", "triple protection", "2026-07-19"]
confidence: 0.66
created: 2026-07-20T04:56:08.220159000+00:00
updated: 2026-07-20T04:56:08.220159000+00:00
---

# 2026-07-19 完整实现批量删除、备份管理与启动脚本并验证 01_cube 完全删除",
  "memory_type": "LongTermMemory",
  "value": "2026年7月19日21:20，用户在 oh-me

2026年7月19日21:20，用户在 oh-memos 记忆管理 GUI（tools/memory-admin/）的基础版上新增了批量删除 cube 功能。前端在 cube 表格中加入复选框和全选按钮（index.html 中的 cubeChk、selAll、toggleAll、updateSel、askBatchDelete），后端提供 POST /api/cubes/batch-delete-preview 用于逐个预览并合计影响，POST /api/cubes/batch-delete 接收 body {cube_ids, confirm}，确认输入 Y 才执行，非 y/yes 返回 HTTP 400。db_admin.delete_cubes 循环调用 delete_cube，对每个 cube 单独备份 JSON 到 backups/，并在每个数据源（Neo4j、Qdrant、文件系统）使用独立的 try‑catch 实现容错。与此同时，用户为同一 GUI 添加了备份管理视图（📦备份管理），前端实现 backupsView、loadBackups、askDeleteBackup，后端提供 GET /api/backups（返回列表并解析 cube_id、节点数、大小、时间）、GET /api/backups/{name}/download、DELETE /api/backups/{name}，并在 db_admin._safe_backup_path 中拒绝包含 / \\ .. 的文件名，以防路径穿越，只删除 backups/ 目录下的文件，不影响数据库已有数据。用户在项目根目录新增 memory-admin.bat，双击即可启动 GUI；脚本执行 chcp 65001 设置 UTF‑8 编码，检测并激活 virtual environment，随后打开 http://127.0.0.1:18010。随后，用户在同一时间验证了 01_cube 的彻底删除：系统自动导出 backups/cube_01_cube_*.json；Neo4j 中对应的 Memory 节点数降至 0；Qdrant 集合 01_cube_graph 被删除；相关目录被清除。删除后 cube 总数从 43 降至 42，Qdrant collections 亦从 43 降至 42。批量/备份删除的 Y 确认门禁与空选择保护均通过返回 HTTP 400 验证。基于上述实现，批量删除功能触发了三层保护工作流：先通过 /api/cubes/batch-delete-preview 干跑预览统计，再要求输入 Y 并在后端验证 confirm 与 cube_id 匹配，最后 db_admin.delete_cubes 在实际删除前自动导出 JSON 备份并对每个数据源的删除操作进行独立容错。该工作流在独立的 memory-admin GUI（通过 .venv/Scripts/python.exe tools/memory-admin/run.py 启动，访问 http://127.0.0.1:18010）中实现，并在完成代码修改后需手动重启 GUI 服务以加载新代码。",
  "tags": [
    "batch delete",
    "backup management",
    "memory-admin GUI",
    "memory-admin.bat",
    "cube deletion",
    "Neo4j",
    "Qdrant",
    "filesystem",
    "HTTP 400",
    "safety",
    "triple protection",
    "2026-07-19"
  ],
  "summary": "用户在2026年7月19日的里程碑更新中，完成了oh-memos记忆管理GUI的三项核心增强：批量删除cube功能、备份管理视图以及可直接双击启动的memory-admin.bat脚本。同时，对01_cube的彻底删除进行了完整验证，确认相关数据、文件和Qdrant集合均已清除，并通过HTTP 400机制保障删除安全。用户还特别提醒，代码修改后需手动重启GUI服务以加载新代码。
