# oh-memos API — production runtime dependencies (Linux / CPU)
#
# Scope: exactly what `oh_memos.api.start_api:app` needs to serve requests in
# tree_text mode. Versions are pinned to the project's own poetry.lock so the
# image matches a resolution the project already tests against.
#
# Deliberately NOT here:
#   torch          — installed separately from the PyTorch CPU index in the
#                    Dockerfile; plain PyPI resolves the CUDA build (~2.5GB of
#                    nvidia-* wheels) on linux/amd64.
#   markitdown     — only used by the document-parser path (magika +
#                    onnxruntime); read_multi_modal/utils.py degrades gracefully
#                    when it is absent.
#   sentence-transformers — pulled in only by an optional langchain splitter
#                    guarded with try/except ImportError.
#   pymilvus, redis, pika, pytest — preference memory, scheduler queues and
#                    tests are not part of this image.

# --- core (pyproject [project].dependencies) ---
openai==1.97.0
ollama==0.4.9
transformers==4.53.2
tenacity==9.1.2
fastapi[all]==0.115.14
sqlalchemy==2.0.41
pymysql==1.1.2
scikit-learn==1.7.0
python-dateutil==2.9.0.post0
prometheus-client==0.23.1
concurrent-log-handler==0.9.28

# --- legacy Python MCP import contract ---
# The active Claude Code integration is the Node/npx package, but fastmcp is a
# declared core dependency in pyproject.toml. Keep it so `pip check` proves the
# published API package has a complete dependency contract.
fastmcp==2.10.5

# --- tree_text memory (pyproject extra: tree-mem) ---
neo4j==5.28.1
schedule==1.2.2

# --- vector store ---
qdrant-client==1.16.2

# --- retrieval: BM25 + CJK tokenisation (imported by tree_text at module load) ---
rank-bm25==0.2.2
jieba==0.42
cachetools==6.2.1

# --- chunking (default chunker backend is "sentence") ---
chonkie==1.1.1
langchain-text-splitters==1.0.0

# --- config loading ---
python-dotenv==1.1.1
